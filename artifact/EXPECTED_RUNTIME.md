# Expected runtime (Apple M2, CPU)

| Target | Typical wall time |
|--------|-------------------|
| `make test` | 1–4 minutes |
| `make smoke` (5 seeds + algo smokes) | 5–20 minutes |
| medium CPU (`medium_cpu.yaml`) | 2–8 minutes |

Times vary with load; these are order-of-magnitude expectations, not SLAs.
