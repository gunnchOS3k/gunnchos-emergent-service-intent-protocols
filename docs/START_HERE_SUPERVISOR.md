# Start here (supervisor) — emergent service-intent protocols

Optional **distributed-intelligence extension**, not a fourth paper. GENOME is a public theme, not an appointment.

1. `docs/CLAIM_BOUNDARIES.md`  
2. UML: `docs/uml/current/index.md`  
3. `REPRODUCIBILITY.md` → `make test` then `make supervisor-cpu-gate`  
4. Expect `results/blocked_gpu/BLOCKED_GPU.json` without CUDA  
5. Visibility: `docs/packets/REPOSITORY_VISIBILITY_PACKET.md`

Do not read message exchange as emergent language. Interpretability (entropy, MI, topographic similarity, interventions, repeated runs) is required even to say *structured signaling candidate*.
